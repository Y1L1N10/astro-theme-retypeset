# GitHub 数据同步与集成计划

本文档旨在规划如何从 GitHub 个人主页挖掘高价值数据，并将其同步到博客中，以增强个人展示效果和内容的动态性。

## 1. 🏆 精选项目展示 (Pinned Repositories)

**目标**: 在博客中自动展示 GitHub 个人主页上"置顶"的精选项目，无需手动维护博客上的项目列表。

*   **数据源**: GitHub GraphQL API (查询 `pinnedItems`) 或 REST API (查询 `repos` 并筛选，但无法直接获取 pinned 状态，通常推荐 GraphQL)。
*   **展示内容**:
    *   项目名称 (Name)
    *   项目描述 (Description)
    *   主要语言 (Primary Language - Name & Color)
    *   Star 数量 (Stargazer Count)
    *   Fork 数量 (Fork Count)
    *   仓库链接 (URL)
*   **应用场景**:
    *   **首页 (Home)**: 在 Hero 区域下方或侧边栏添加 "Featured Projects" 模块。
    *   **项目页 (/projects)**: 自动生成项目列表页面。
    *   **文章侧边栏**: 随机推荐一个自己的开源项目。

## 2. 📊 技术栈可视化 (Tech Stack Statistics)

**目标**: 通过分析代码仓库的语言分布，可视化展示个人的技术栈构成。

*   **数据源**: GitHub API (遍历所有公共仓库的 `languages` 字段并聚合计算)。
*   **展示内容**:
    *   语言名称 (e.g., TypeScript, Rust, Python)
    *   使用占比 (Percentage)
    *   代码行数/字节数 (可选)
*   **应用场景**:
    *   **关于页 (/about)**:
        *   **饼图/环形图**: 直观展示语言分布。
        *   **进度条**: 展示 Top 5 语言的熟练度（基于代码量）。
    *   **页脚 (Footer)**: "Most used languages" 标签云。

## 3. ⚡️ 实时动态流 (Recent Activity Stream)

**目标**: 展示博主最近的编码活动，证明博客背后的开发者是活跃的。

*   **数据源**: GitHub Events API (`/users/{username}/events`).
*   **展示内容**:
    *   **PushEvent**: "Pushed 3 commits to `repo-name`"
    *   **WatchEvent**: "Starred `repo-name`"
    *   **PullRequestEvent**: "Opened PR #42 in `repo-name`"
    *   **ReleaseEvent**: "Released v1.0.0 of `repo-name`"
*   **应用场景**:
    *   **侧边栏 (Sidebar)**: "Recent Coding Activity" 时间轴小组件。
    *   **首页底部**: 滚动显示的动态条。

### 3.1 ⚡️ 实时动态流详细开发计划

#### 1. 数据获取层 (`src/utils/github-activity.ts`)
*   **功能**: 封装 GitHub Events API 调用，处理数据清洗和缓存。
*   **接口定义**:
    ```typescript
    interface ActivityItem {
      id: string
      type: 'PushEvent' | 'WatchEvent' | 'PullRequestEvent' | 'CreateEvent' | 'ReleaseEvent'
      repo: { name: string, url: string }
      payload: any // 根据 type 不同而不同
      created_at: string
    }
    ```
*   **核心逻辑**:
    *   使用 `fetch('https://api.github.com/users/{username}/events')` 获取最近 30 条动态。
    *   **过滤**: 只保留核心关注的事件 (Push, Star, PR, Release)，过滤掉噪音 (如 DeleteEvent)。
    *   **格式化**: 将原始 JSON 转换为前端易用的 `ActivityItem` 结构。
    *   **缓存**: 实现简单的内存缓存或构建时静态数据 (SSG)，避免频繁请求触发 API 限制。

#### 2. UI 组件层
*   **`src/components/Widgets/ActivityCard.astro`**:
    *   单条动态的展示组件。
    *   包含：事件图标 (Commit/Star/PR)、仓库名称 (带链接)、描述文本 (e.g., "pushed to master")、相对时间 ("2h ago")。
    *   样式：极简设计，左侧时间轴线，右侧内容。
*   **`src/components/Widgets/ActivityStream.astro`**:
    *   容器组件，负责调用 `getRecentActivity` 获取数据。
    *   渲染 `ActivityCard` 列表。
    *   处理 Loading 和 Error 状态 (如果是客户端渲染)。
    *   **位置**: 计划放置在 `src/layouts/Layout.astro` 的 `<aside>` 区域，位于 `Navbar` 和 `Footer` 之间。

#### 3. 样式与交互
*   **动画**: 列表项载入时的淡入动画。
*   **图标**: 使用 Iconify 或现有图标库区分不同类型的事件。
    *   Push -> `ri:git-commit-line`
    *   Star -> `ri:star-line`
    *   PR -> `ri:git-pull-request-line`
*   **响应式**: 在移动端隐藏或折叠展示。

#### 4. 实施步骤
1.  创建 `src/utils/github-activity.ts` 并实现 API 请求与数据处理。
2.  创建 `src/components/Widgets/ActivityCard.astro` 基础样式。
3.  创建 `src/components/Widgets/ActivityStream.astro` 并集成数据。
4.  修改 `src/layouts/Layout.astro`，将组件加入侧边栏。
5.  调整样式细节，确保与现有主题风格一致 (字体、间距、颜色)。

## 4. 🏅 社交影响力徽章 (Social Proof Badges)

**目标**: 用数据量化开源影响力，建立访客信任感。

*   **数据源**: GitHub User API (`/users/{username}`) + 仓库聚合计算。
*   **展示内容**:
    *   **Total Stars**: 所有仓库获得的 Star 总数（需聚合计算）。
    *   **Followers**: 关注者数量。
    *   **Organizations**: 所属组织/公司的 Logo。
*   **应用场景**:
    *   **Hero Section**: 标题旁的动态徽章 (e.g., "⭐️ 1.2k Stars").
    *   **关于页**: "My Organizations" 区域展示组织 Logo 墙。

## 5. 🧩 README 同步 (Profile README Sync)

**目标**: 复用 GitHub 个人主页精心编写的 README，避免多处维护简介。

*   **数据源**: GitHub Raw Content (`raw.githubusercontent.com/{username}/{username}/main/README.md`).
*   **展示内容**: 完整的 Markdown 内容（包含自我介绍、联系方式、动态 GIF 等）。
*   **应用场景**:
    *   **关于页 (/about)**: 直接渲染 README 内容作为关于页的主体。

## 📅 实施路线图 (Roadmap)

### Phase 1: 核心展示 (High Priority)
- [ ] **集成 Pinned Repositories**: 实现 GraphQL 查询，获取置顶项目数据。
- [ ] **开发 ProjectCard 组件**: 设计精美的卡片样式展示项目信息。
- [ ] **部署到首页**: 在首页添加 "Featured Projects" 区域。

### Phase 2: 数据可视化 (Medium Priority)
- [ ] **聚合语言统计**: 编写脚本在构建时拉取并计算语言分布。
- [ ] **开发 TechStack 组件**: 使用图表库 (或纯 CSS) 渲染语言分布图。
- [ ] **集成到 About 页**: 丰富关于页内容。

### Phase 3: 动态与社交 (Low Priority)
- [x] **实现 Activity Stream**: 获取最近动态并在侧边栏展示。
- [ ] **添加 Social Badges**: 计算并展示 Total Stars 和 Followers。

## 🛠 技术方案备注

*   **API 选择**: 推荐使用 **GitHub GraphQL API**，因为它可以在一次请求中获取 Pinned Repos、语言统计和用户信息，减少请求次数并避免 REST API 的速率限制。
*   **构建时获取 (SSG)**: 所有数据应在 **构建时 (Build Time)** 获取并缓存，而不是在客户端请求。这样可以保证页面加载速度，且不会暴露 GitHub Token。
*   **Token 管理**: 需要申请一个 GitHub Personal Access Token (Fine-grained 权限即可)，并在 CI/CD 环境变量中配置。
